---
status: done
priority: medium
area: misc
labels: [retro]
---

# セッション振り返り 2026-05-26

## うまくいったこと

- milestone フィールドの実装自体は正しく動いた（issue.rs・cli.rs・create.rs・list.rs・readme.rs・spec・README 更新）
- issue 12・14 を pending に変更できた

## 失敗・見落とし・やり直し

- **コミット前にレビューとカバレッジ確認をしなかった**: milestone 実装をそのままコミット・issue close してしまった。ルールが CLAUDE.md に書かれていなかったのが原因だが、事前に確認すべきだった
- **CLAUDE.md を直接編集した**: `.claude/` の変更は self-improve 経由のみというルールが fbim の CLAUDE.md に書かれていなかった。docs の CLAUDE.md には明記されている
- **retro を書かずに self-improve を呼んだ**: self-improve.md に「retro がなければ中断する」と書いてあるのに、retro なしで呼んだ。self-improve 自身もそれをチェックせず実行してしまった
- **スキル名を間違えた**: CLAUDE.md に `/code-review` と書いたが、正しくは `review` サブエージェント（`Agent(subagent_type="review")`）
- **review.md にカバレッジ確認がない**: 「カバレッジは実装時とレビュー時の両方で確認する」と CLAUDE.md に書いたが、review.md のチェック項目にカバレッジが含まれていない

## 指示ファイルにあればよかったこと

- **CLAUDE.md に明記すべきルール**:
  - `.claude/` の変更は self-improve 経由のみ。必ず retro を先に書く
  - 実装フローの明文化（実装 → カバレッジ → review → コミット → close）
  - ベストプラクティスを先に調べてから判断する
- **review.md に追加すべき観点**:
  - カバレッジ確認（`cargo llvm-cov --summary-only -- --test-threads=1`）

## OSS マーケ・公開戦略エージェントの追加

marketing-strategist / docs-writer / launch-orchestrator の3エージェントを追加。
参照資料を WebFetch で調査し self-improve 経由で .claude/agents/ に反映した。
